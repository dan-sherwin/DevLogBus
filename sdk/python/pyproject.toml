[project]
name = "devlogbus"
version = "0.1.0"
description = "Python SDK for publishing records to DevLogBus."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
    { name = "Dan Sherwin" }
]
